# λ 区间带方案:自适应 τ 把 credit 权重框进 (1/λ, λ)

> 背景:VPO p9g 坍缩分析([vpo坍缩分析-p9g.md](vpo坍缩分析-p9g.md))确认病根是"credit 集中 = 热点 token 有效步长放大 10–20 倍"。本方案(同学提出)用逐响应自适应 τ 把权重硬性框进区间带,λ 为唯一超参数。

---

## 一句话结论

**方案正确命中病根,且比我当前的"降 lr"补丁更治本**:λ 把有效步长放大器限制在 λ 倍以内(实测现状 w_max 14–19,λ=2 是数量级收紧);同时 λ=1 时严格退化为 GRPO,让 VPO 变成 GRPO 的单参数插值族——超参数更少、实验叙事更干净。**但它只治更新层这一层**:重复吸引子(采样层)仍需 presence_penalty,长度偏好(奖励层)仍需去偏,三层防护各就各位。

## 1. 方案是什么

在 Plan B 的标准化 utility($u_t = A\cdot\hat d_t$)之后,不再用固定 τ'=1 进 softmax,而是**逐响应解出自适应温度** $\tau_i$,使得:

$$\max_t\ T\cdot\mathrm{softmax}_t(u/\tau_i)\ \le\ \lambda,\qquad \min_t\ T\cdot\mathrm{softmax}_t(u/\tau_i)\ \gtrsim 1/\lambda$$

即:任何 token 的 credit 权重最多是均匀值的 λ 倍、最少不低于一半(λ=2 时)。τ_i 与集中度单调(τ 越大越平),所以永远有可行解(τ→∞ 退回均匀);**若标准化后的自然分布本来就在带内,τ_i 保持 τ'=1 不动——自适应只收紧、不放松,保留方法的设计意图**。

## 2. 为什么能治 VPO 的病

死亡链条(见尸检文档):credit 集中 ⇒ 热点 token 有效步长 ×(10–20) ⇒ 过冲 ⇒ 钻进高先验重复吸引子(换行刷屏)⇒ 熵死锁。

| 链环 | λ 如何切断 |
|---|---|
| 有效步长放大 | w ≤ λ 直接把放大器上限从 ~20 压到 2,过冲幅度回到 GRPO 量级 |
| 吸引子自增强 | 步长小 ⇒ 单步钻不深;且下界 w ≥ 1/λ 保证坏 token 仍有足额负 advantage,纠错信号不断 |
| 熵死锁 | 更新漫射 ⇒ 熵保持(p9g 里 GRPO 同地形存活即证明该量级更新安全) |

## 3. 比现有手段好在哪

| 现有手段 | λ 方案 | 优势 |
|---|---|---|
| weight_cap=20(硬截断 + 水填充重归一) | 自适应 τ 平滑压进区间 | 无截断不连续点,梯度动力学干净 |
| τ'=1 手调 + VPO 专属 lr 3e-5(两个补偿旋钮) | λ 一个超参数,lr 与 GRPO 共享 | 少一个自由度;"稳定器长在方法内部"是更干净的结论 |
| 截断后冷 token 权重趋 0,负信号变弱 | 下界 1/λ 保住冷 token 的惩罚份额 | 保住 credit 的"压差"功能 |

**最有价值的性质:λ = 1 严格等于 GRPO。** VPO 变成 GRPO 的单参数插值族,λ ∈ {1, 2, 4, 8} 扫参即得 credit 集中度的剂量-响应曲线——直接回答"方法的价值和风险各在多大集中度出现"。

## 4. 不能解决什么(诚实清单)

- **采样层的重复吸引子**:换行刷屏是生成侧问题,λ 只是让策略走得更慢,presence_penalty=0.3 仍然必要
- **奖励层的长度偏好**:去偏(0.00506/tok @ anchor 600)与超长/过短 floor 仍然必要
- **奖励地形的其它坑**:RM 的任何残留缺陷仍会被慢慢找到,监控(熵/KL/长度三面板)仍然必要

## 5. 具体实施

### 数学与算法

标准化后的 logits 记 $u_t$(离散度 ≈ |A|,典型 1–3)。对每个响应二分求解 τ_i:

```
初始化 τ ∈ [0.05, 50](对数空间)
重复 ~40 次:
    w = counts · softmax(u / τ_mid)
    若 max_t w > λ:τ 太小(太集中)→ 抬高下界
    否则:降低上界
取可行侧的 τ;若 τ 解 > τ'=1,说明自然分布在带内,用 τ'=1
min ≥ 1/λ 一般随 max 收紧自动满足(分布整体变平);实现中顺带校验,
违例则继续放大 τ(实践中 max 几乎总是先绑死)
```

计算量:[B, T] 上的 40 次 softmax,相对词表分块的 $d_t$ 计算(151k 维 × 分块)可忽略。

### 代码落点(全部在 `vpo_rm/core.py:allocate()`,~30 行)

1. 标准化 → u 之后,加区间带二分;**删除** water-filling 截断块(失业)
2. `weight_cap` 配置退役,新增 `credit_lambda: float = 2.0`(TrainerConfig / CLI 同步)
3. metrics 新增 `credit_tau_adaptive_mean`:解出的 τ_i 均值——**诊断金矿**,它量化"数据想要多大集中度、被 λ 压制了多少"
4. 良性质:区间带保证 ESS_ratio ≥ ~1/λ² ≈ 0.25,与 0.5–0.95 监控工作区自洽

### 测试(改三处 + 加一条金标准)

- 权重上界断言:20 → λ
- **金标准:λ=1 时 credit 与 GRPO 逐元素相等**(方案正确性的最强单测)
- 自适应 τ 单调性:λ 越小解出的 τ 越大
- 保留:保均值、零 advantage、退化方向回退三条不变式

### 实验安排(不推翻现状)

| run | 配置 | 回答的问题 |
|---|---|---|
| p9h(在跑) | lr 3e-5 + cap 20 + presence_penalty | "优化器空间补偿"路线能不能活 |
| **p9i** | **lr 5e-5(与 GRPO 共享)+ λ=2 + presence_penalty** | "方法内稳定器"路线能不能活;若活,结论更干净 |
| p9j+ | λ ∈ {4, 8}(若 p9i 活) | 集中度甜点/悬崖在哪 |

### 风险与边界情况

- λ=2 时方法与 GRPO 差异很小(带宽仅 4×)——**这正是剂量-响应实验要测的东西**,不是缺陷;若 λ=4 也稳定且收益更高,甜点在 4
- A=0 响应:照旧全零,不受影响
- 全同 d̂(退化方向):照旧回退均匀,不受影响
- bisection 的数值稳定:标准化后 u 量级有界(≈|A|≤~3),τ 搜索区间 [0.05, 50] 绰绰有余

## 6. 给作者的一句话版本

> "把 weight_cap 的硬截断升级成区间带 (1/λ, λ):逐响应自适应 τ 让 softmax 平滑地满足上界,λ=1 时严格退化为 GRPO。这样 VPO 成为 GRPO 的单参数插值族,集中度-稳定性-收益三者的关系可以用一次 λ 扫参讲清楚;而'有效步长放大器被 λ 封顶'正是 p9g 三连死的确切病根。"

## 一页总结

| 问题 | 答案 |
|---|---|
| 能否解决 VPO 的问题 | 能,精确切断"有效步长放大 → 过冲 → 吸引子 → 熵死锁"的第一环;λ=2 把放大器从实测 14–19× 压到 2× |
| 比现有方案好在哪 | 平滑(无截断不连续)、单超参、λ=1≡GRPO 构成插值族、保住冷 token 的负信号 |
| 不能解决什么 | 采样层吸引子(presence_penalty 仍需)、奖励层长度偏好(去偏仍需)、RM 其它坑(监控仍需) |
| 怎么实施 | `allocate()` 内标准化后二分解 τ,~30 行;weight_cap 退役、credit_lambda 上岗;新 metric credit_tau_adaptive_mean;金标准单测 λ=1≡GRPO |
| 实验路径 | p9h 跑完(优化器路线)→ p9i(λ=2, lr 共享 5e-5)→ 若活,扫 λ∈{4,8} |
