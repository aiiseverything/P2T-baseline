# Llama RL implementation and launch plan

> For agentic workers: use superpowers:subagent-driven-development; preserve existing SFT changes and independently review each task.

**Goal:** Audit the actual Llama actor and Skywork RM protocols, adapt the existing Qwen RL pipeline, and run four experiments: GRPO, VPO lambda2/4/8 initialized from the verified Llama SFT adapter.

**Architecture:** Keep the existing trainer, credit math, length reward, and vLLM profile runner. Correct model-specific serialization/padding in shared paths; add a separate Llama launcher and explicit protocol verification. Freeze runtime sources and identities before allocating formal training resources.

**Tech stack:** Python/PyTorch/Transformers/PEFT/vLLM, pinned existing GPU image, 3 dedicated GPUs per arm (actor/RM/rollout).

**Spec:** User objective in this thread; protocol decisions and evidence are recorded below and in the final audit document. User has authorized development and the four rjobs; routine implementation decisions are covered by that authorization.

## Global constraints

- Worktree: /data/VPO-RM/code, existing llama-sft-20260917 branch. Preserve all uncommitted SFT changes and all original frozen Qwen/Arena artifacts.
- Actor: /data/VPO-RM/models/Llama-3.1-8B-Instruct. RM: /data/VPO-RM/models/Skywork-Reward-Llama-3.1-8B-v0.2.
- Initial adapter: /data/VPO-RM/models/sft-llama31-8b-instruct-clean2k5e2-20260917. Protect existing weights; training and frozen KL adapter must both start from these exact tensors.
- Baseline configuration: runs/rl-fp32-is-canonical-20260917/experiment.json. Preserve 250 rollouts, 8 prompts x8 responses, 2048 response cap, lr5e-5, KL beta.03 to SFT init, one policy epoch,64-response optimizer minibatch, seed42/generationseed0, FP32 policy head and detached token IS.
- Length protocol: short8/long1024/max2048, strengths.5/2, advantage std floor.5; calibrate fresh Llama sigma0 on128prompts x8responses and share it across all four arms.
- VPO lambda2/4/8 freezes stop and structural token credit at1; GRPO uses uniformcredit.
- Runtime: Torch2.13.0+cu129, Transformers5.16.1, PEFT.20.0, PyArrow21.0.0, vLLM0.28.1rc1.dev199+g7c5dc571c.cu129; VLLM_WORKER_MULTIPROC_METHOD=spawn. Do not install heavy packages on GPU jobs.
- /data is NFS10.68.62.222:/data, explicit rjob mount required. ~122GiB free at initial audit. Four formal jobs only; run shared validation/calibration inside the first job before launching the remaining arms where feasible, avoiding idle reserved GPUs.
- Failed validation must prevent formal training. New sources and inputs are hash-bound; paid judgments/Arena tasks remain independent.

## Task 1: actor protocol and rollout consistency

Files: vpo_rm/token_policy.py, vpo_rm/trainer.py, scripts/profile_vllm_full.py, scripts/vllm_generate_server.py, scripts/preflight_training.py; focused tests.

- [x] Regression reproduces exactly-one-BOS violation in _encode_prompts on a tokenizer that adds BOS by default.
- [x] Load/validate the initialized actor tokenizer where saved, preserve native chat template and fixed date, choose model-owned dedicated padding128004 for Llama rather than EOS128009. RM padding must be configured independently.
- [x] Encode rendered chats with add_special_tokens=False; carry exact prompt token IDs into vLLM instead of implicit re-tokenization. Verify HF/vLLM prompt IDs in every RPC and in preflight checks.
- [x] Stop IDs are registered128001/128008/128009, pad is suppressed from output, no unknown ID fallback, generated nativeEOT is preserved exactly once. Verify FP32head and PEFTdefault/ref behavior.
- [x] Run focused regressions plus existing Qwen tokenization/profile tests; independent review.

## Task 2: RM serialization and byte mapping

Files: vpo_rm/reward_inputs.py; tests/test_reward_inputs.py and Llama-specific tests.

- [x] Regression proves canonical RM full-chat IDs are correct but response mapping is all-1 because stripped BOS text differs from canonical token bytes.
- [x] Preserve native rendered BOS for byte offsets while still tokenizing the RM model-card format with exactly one BOS; retain conservative ID+byte-span mapping, terminal specials unmapped.
- [x] Real tokenizer checks cover ASCII, Chinese/emoji, whitespace/newlines, repeated text, empty answer, EOS removal, and long prompt boundaries. Preserve Qwen behavior.
- [x] Validate RM raw scalar/no sigmoid, last nonpadding token, own padding128004, no response truncation, frozen RM parameters with nonzero input gradient. GPU batch/single and directRM/wrapper parity are launch gates.

## Task 3: Llama experiment controller and verification

Files: new scripts/llama_rl_launcher.py, new protocol-check module/script and focused tests; protocol audit doc.

- [x] Derive common settings from the frozen Qwen configuration with only actor/RM/SFT/paths changed; no reuse of Qwen sigma0.
- [x] Record CPU protocol report, dataset/split identity, SFT completed/reloaded evidence, savedtokenizer identity, modeldownload verification, source hashes and rjob runtime.
- [x] Submit exactly four formal arm jobs. First job performs protocol GPU parity + four-arm two-rollout numerical gates + fresh shared calibration, then starts its formal arm. Remaining arms only start after shared validation succeeds; no loading of previous Qwen validation as proof.
- [x] Every arm validates immutable inputs/runtime, frozenref remains unchanged, trainedadapter updates, exactprompt IDs, finite/nonzero gradients, credit coverage, sampled/HF logprob tolerances, stop/length metadata, initial quality, and long-sequence memory capacity.
- [x] Keep step0/current/final adapters, finalcheckpoint250; write durable stage/startup/completion/error artifacts and preserve logs on failure.
- [x] Controller and protocol behavior tests run before implementation is declared complete; inspect independent review findings before launch.

## Task 4: launch and verify experiments

- [x] Verify disk free on actual NFS target and scheduler resources; freeze code/config/audit inputs.
- [x] Submit first arm with shared validation; inspect real rjob/GPU runtime and reports, repair any actual failure without modifying running frozen sources.
- [x] Submit other three arm jobs after shared gates, verify scheduler registration and initialupdates for each, equalSFT/sharedsigma0/configexceptmethod/lambda.
- [x] Monitor all four experiments through requested run completion; validate finalcheckpoint250,250rollouts, finite metrics, and persistent artifacts before marking the full goal complete.

## Findings / decisions

Initial audit: _encode_prompts adds a second LlamaBOS; RM canonical IDs are correct but _render stripsBOS before byte mapping causing all responsepositions=-1; trainer/server override nativepadding withactorEOS. Native vLLM pinned generic LogitsProcessor supports FP32head forLlama. CPUTransformers4.57 cannot AutoTokenizer-load TF5 TokenizersBackend exports, so productionverification uses pinnedTF5; CPUnativeprotocolchecks must explicitly load fasttokenizer artifacts or baseequivalence without pretending the oldruntime is formal.

Ruling: Keep the authorized full experiment scope and existing corrected RL math. Fix only proven protocol gaps and add a separate model-specific launcher. No additional approval is needed for these reversible changes or the explicitly requested jobs.

## Verification checkpoint (2026-09-18 01:16 HKT)

- Full CPU regression: 947 passed, 4 skipped, 5 warnings; sources unchanged throughout the run. Evidence: /data/VPO-RM/.maintenance/llama-rl-20260918/cpu-regression.json and cpu-regression.log.
- Actual-tokenizer CPU protocol report passed: /data/VPO-RM/.maintenance/llama-rl-20260918/cpu-protocol.json.
- Independent actor/RM/launcher review completed; discovered and fixed capacity prompt duplicate BOS and actual RM token expansion beyond the input budget.
- Full actor/RM/SFT SHA256 revalidation passed; native input/template identities and exact training-prompt audit passed.
- Exact normalized Arena-Hard prompt overlap audit found zero matches in the retained UltraFeedback train/validation pools. This is not a semantic contamination or pretraining audit.
- Suite preparation started at /data/VPO-RM/runs/llama31-rl-canonical-20260918. Real GPU gates and formal jobs remain pending.

## Verification checkpoint (2026-09-18 02:31 HKT)

- The current immutable suite is `/data/VPO-RM/runs/llama31-rl-canonical-20260918-v3`. Its experiment SHA256 is `ead130be71d0c0fa9002c6ca37502f4c992d76dcb1d7b42535dd2281b5f4a6e8`. Earlier failed suites and the isolated diagnostic remain preserved.
- Final CPU regression passed 972 tests with 4 skips; all 120 checked source files remained unchanged. The suite's `audit/tf5-regression-status.json` records the result.
- The real H200 RM protocol gate passed: identical BF16 inputs agree exactly across native/wrapper/gradient paths, and full-FP32 padding invariance differs by at most 0.0000319481 with TF32 disabled. The separate BF16 cross-layout difference is retained as a diagnostic; production RM microbatch size is required to be one. See `gpu-protocol.json` and `audit/gpu-protocol-root-review.json`.
- The live GRPO job is `llama-rl-grpo-3e1b3385dd-41227307`. Scheduler and pod process checks confirm that shared training preflight is running. Actor and RM have occupied GPUs 0 and 1; the vLLM child has started initialization. Fresh calibration, all four two-rollout checks, lambda8 capacity, and formal training are still pending.
- `/data` has approximately 121 GiB free. A bounded read-only weight prewarm completed all RM shards and part of the actor shards before its 180-second limit; only its helper exited. Touched files retained their size/mtime/inode. Actor cold reads measured about 42 MiB/s; the cached RM sample does not establish cold-storage throughput. See `audit/weight-prewarm.json`.
- The remaining three formal jobs are prepared but unsubmitted. Their submission depends on a validated `shared-gate.json`, and completion of this plan still requires all four 250-rollout checkpoints and unchanged frozen references.

## Verification checkpoint (2026-09-18 02:58 HKT)

- Fresh shared calibration passed: 128 distinct prompts, 1,024 responses, sigma0 6.166627762618061. Root's independent arithmetic exactly reproduced it.
- GRPO and lambda2/4/8 all passed two actual optimizer updates, changed-adapter/unchanged-reference checks, all three probability probes, and objective quality gates. Same prompts and actual resolved configurations were independently compared; evidence is `audit/four-preflight-rollouts-root-review.json`.
- Lambda8 also passed two consecutive 64x2048 capacity updates, actor inputs64x4095 and all RM inputs4095. Root and independent reviewer checked actual shape, memory, tensor digests, and isolation of synthetic-only IS settings.
- The frozen launcher validated the complete shared gate before root submitted the other arms. All four job IDs are recorded in `all-submissions.json`, and all four registrations were queried successfully. GRPO is in its formal training stage; lambda2/4/8 are queued/starting.
- Required work remaining: observe each formal job's runtime and initial-update validation, confirm its actual SFT/sharedsigma/250-rollout configuration, then monitor all four through finalcheckpoint250 and reference-integrity verification. The goal remains active.

## Verification checkpoint (2026-09-18 03:41 HKT)

- All four formal startup validations have passed. Producer-side actual runtime/configuration records match the protected SFT, shared sigma0, 250 rollouts, seeds, FP32 policy head and importance correction; only method/lambda, corresponding freeze switches and output paths differ. Evidence: `audit/all-formal-startups-root-review.json`.
- All four saved formal step-0 adapters match the protected SFT exactly: 448 FP32 tensors and 167,772,160 elements per arm, with no key, shape, dtype or value differences. Evidence: `audit/formal-step0-sft-comparison.json`.
- Growing NFS log files can appear stale from the controller host. The live monitor therefore queries the scheduler and reads profile/status files on each producer pod; previous local observations are preserved with the correction documented in `audit/live-monitor/monitor-correction.json`.
- At 03:40:59 HKT, fresh producer observations report GRPO38/lambda2-20/lambda4-18/lambda8-12, all Running with no failure artifacts or nonfinite metric rows. `/data` has about 90.6 GiB free. These interim observations do not establish final training quality or completion.
- Required work remaining: monitor all four runs through rollout250 and verify final checkpoint completeness, adapter updates, unchanged references and protected SFT integrity. No final checkpoint is claimed complete here.

## Verification checkpoint (2026-09-18 05:43 HKT)

- All four arms have completed the same first100 prompt batches: 6,400 responses and100 optimizer updates each, with no skips. Producer snapshots and root's independent arithmetic verify finite profile/reward values, RM input budget, counts/lengths/rewards, token-weighted entropy/KL and logged VPO mean/max weights. Evidence: `audit/first100-health/`; 72 all100/last10 summary fields independently agree.
- Rollouts91–100 have3/3/3/2 capped responses per640 and token-weighted entropy0.8067/0.9125/0.8483/0.8485. Lambda8's higher KL remains under observation. These training statistics do not establish held-out quality or rule out general repetition.
- A producer-side directory check confirmed only step0/current adapters per arm and approximately90.08GiB free; intermediate checkpoints have not accumulated. Evidence: `audit/retention-20260918-0533.json`.
- Lambda2 rollout99's4,109 unmapped content tokens were traced to two capped answers ending in incomplete UTF-8 (4,096 tokens) plus13 local BPE merge mismatches. Producer CPU reconstruction and root's saved-credit/hash verification passed; all fallback positions have weight1/direction0 and retain sequence advantage. Evidence: `audit/lam2-rollout99-mapping-review/`. This does not re-observe unsaved RM-forward input-ID tensors; no training changes were made.
- The four jobs remain active. Final verification must inspect the actual checkpoint250 artifacts, all250 rollout records, finite/changed trained weights, frozen reference keys/shapes/dtypes/values, optimizer state and protected SFT hash before the outstanding task checkboxes can be completed.

## Verification checkpoint (2026-09-18 07:10 HKT)

- A final-verifier review found that legitimate whole-rollout skips omit `grad_norm`, while the frozen completion validator requires it unconditionally. A development-only minimal fix accepts the missing field only for skipped rollouts; updated rollouts still require it, and any supplied gradient/loss must remain finite. A failing regression reproduced the issue before the fix; the launcher/trainer/lifecycle checks then passed115 tests with3 warnings. Independent review found no blocking issues. Evidence: `audit/completion-validator-skip-fix.json` and `.patch`.
- Producer observations at07:10 report215/187/177/178 completed rollouts, with no whole-rollout skips or missing gradient fields in any arm. All292 frozen source hashes still match the experiment manifest. No formal job or frozen source was changed. Evidence: `audit/skip-policy-observation-20260918.json`.
- The concrete remaining checks, artifact paths and evidence limitations are recorded in `audit/final-verification-requirements.md`. In particular, final optimizer state, reference dtype, adapter configs, tokenizer and raw-log/summary consistency require independent checks beyond the launcher. No final checkpoint is accepted yet.

## Final verification checkpoint (2026-09-18 08:50 HKT)

- Fresh scheduler queries confirm all four formal jobs Succeeded. Each completed 250 rollouts, 250 optimizer updates and zero skips, with exit code 0 and a saved `train/checkpoint-250`.
- Independent metadata checks passed for all 250 rollout records per arm, prompt order, token/stop/padding semantics, reward arithmetic, profile/summary consistency, configurations, tokenizer identity and all 292 frozen source hashes. Each arm generated 16,000 responses.
- Independent final tensor checks passed for trained/reference adapters, optimizer states and config/RNG structure. All 448 trained FP32 tensors changed in each arm; reference tensors remain exactly equal to the protected SFT. Input hashes were unchanged before and after verification. Optimizer ID-to-parameter-name binding and resumable-training behavior were not independently demonstrated.
- Root reviewed all four metadata and tensor reports against completion artifacts. Durable evidence is in `/data/VPO-RM/runs/llama31-rl-canonical-20260918-v3/audit/FINAL_TRAINING_REPORT.md` and `final-root-review.json`; final scheduler status is in `final-scheduler.log` and `live-monitor/final.json`. The SSH observer was stopped only after all four jobs succeeded.
- All requested protocol, implementation, launch and final training verification work is complete. These checks establish training completion and artifact integrity; held-out benchmark quality remains a separate measurement.
