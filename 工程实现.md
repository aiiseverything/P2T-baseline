# VPO-RM：工程实现与 UltraFeedback 实验

> 2026-09-16：审查修复后的行为、历史结果影响和复现边界见 [项目修复说明](docs/project-audit-fixes-2026-09-16.md)。下文含历史实验设计，实际运行配置以各次 manifest 为准；已有 checkpoint 尚不支持完整断点恢复。

使用冻结 RM 的输入梯度构造 token credit，通过 GRPO 更新 Qwen3-14B 的 LoRA 参数。当前实验比较 **GRPO 与 VPO-RM**，采用两种 RM，统一使用种子 42，共 4 次训练。数学展开见 [数学原理](数学原理.md)，参数见 [experiments.json](configs/experiments.json)。

## 1. 模型与 tokenizer

| 设置 | Actor | 冻结 RM | 训练任务 |
|---|---|---|---|
| ultrafeedback_themis_14b | Qwen/Qwen3-14B + LoRA | project-themis/Themis-RM-14B | UltraFeedback 通用指令对齐 |
| ultrafeedback_skywork_8b | Qwen/Qwen3-14B + LoRA | Skywork/Skywork-Reward-V2-Qwen3-8B | UltraFeedback 通用指令对齐 |

两种 RM 都通过标量 score head 输出整条回答的奖励。Themis 具有通用偏好预训练和代码偏好训练阶段；Skywork V2 面向通用偏好。每个 RM 设置分别训练 GRPO 和 VPO-RM，比较同一奖励来源下 credit 分配的效果。[Themis](https://huggingface.co/project-themis/Themis-RM-14B)、[Skywork](https://huggingface.co/Skywork/Skywork-Reward-V2-Qwen3-8B)

**Actor 与 RM 使用相同的 tokenizer：分词规则、token→ID 映射和特殊 token 语义一致。** 概率 $p_t(b)$ 直接对应 RM embedding 矩阵第 $b$ 行；两者的隐藏维度、参数量和 embedding 权重可以不同。

两组 tokenizer 统一使用 `<|endoftext|>` 作为 padding，并同步模型和 generation config 的 `pad_token_id`。加载后执行完整检查：

```python
for tokenizer in (actor_tokenizer, rm_tokenizer):
    tokenizer.pad_token = "<|endoftext|>"
check_tokenizers(
    actor_tokenizer, rm_tokenizer,
    actor.get_output_embeddings().weight.shape[0],
    rm.get_input_embeddings().weight.shape[0],
)
```

元数据检查记录见 [tokenizer_audit.json](configs/tokenizer_audit.json)。训练记录固定模型 revision，并在实际加载时再次核对。

Actor 使用 `enable_thinking=False` 的聊天模板。RM 使用对应 checkpoint 的模板；将生成的回答 token ID 直接嵌入 RM 输入，保存回答位置映射。Actor 的预测 logits 位于回答 token 的前一个位置，RM 梯度位于该 token 自身的位置。模板前后缀计入长度预算，score pooling 选择 RM 输入最后有效位置。

`shared_output_mask` 定义共同的有效输出词表，生成、旧策略概率、参考策略概率和更新策略概率使用相同支持集。prompt 与 padding 通过 mask 处理，生成的 EOS 参与 policy loss。

## 2. UltraFeedback 数据与评价

使用 [HuggingFaceH4/ultrafeedback_binarized](https://huggingface.co/datasets/HuggingFaceH4/ultrafeedback_binarized) 的 `train_prefs`，提取 `prompt`。在线回答由当前 Actor 采样，训练奖励由当前设置的 RM 提供。

数据处理顺序：

1. 对 prompt 做 Unicode NFC 和空白规范化，以此生成 SHA256 去重键；模型输入保留原始文本。
2. 与最终评价集按规范化文本核对重叠，过滤重复 prompt。
3. 分别套用 Actor、RM 模板，保留双方 prompt 长度均在 2,048 token 预算内的样本，并为回答保留 2,048 token。RM 模板后缀计入 prompt 侧开销。
4. 按去重键排序，前 2,000 条作为验证集，其余作为训练集。保存数据 revision、最终样本 ID 和 split hash。

两种 RM、两种方法共享最终训练集合。各方法使用相同的 prompt 顺序；遍历结束后重新洗牌。验证集用于记录训练趋势，主表采用第 500 次 rollout 的 checkpoint。

| 评价集 | 指标 | 关注能力 |
|---|---|---|
| [AlpacaEval 2](https://github.com/tatsu-lab/alpaca_eval) | length-controlled win rate | 通用回答偏好 |
| [Arena-Hard v2](https://github.com/lmarena/arena-hard-auto) | 官方胜率，分别报告 hard 与 creative writing | 复杂开放任务、创意写作 |
| [IFEval](https://github.com/google-research/google-research/tree/master/instruction_following_eval) | strict prompt / instruction accuracy | 可程序验证的指令遵循 |

评价统一使用非思考模式、温度 0.7、top-p 0.9，每题生成一次，回答上限 2,048 token。保存最终 prompt、回答和生成参数；记录评价 prompt 长度及截断情况。裁判使用官方 evaluator 配置，固定 evaluator commit、裁判模型版本和模板，并单列 API 费用。训练 RM 分数作为训练指标，独立评价用于衡量实际质量。

## 3. 两张 H200 与 LoRA

| 组件 | 配置 |
|---|---|
| GPU 0 | Qwen3-14B BF16 基座、LoRA、优化器；分阶段生成与更新 |
| GPU 1 | BF16 RM；序列评分、输入反向与 credit 计算 |
| LoRA | $r=64$，$\alpha=128$，dropout 0，bias none |
| 目标模块 | q/k/v/o projection，gate/up/down projection |
| 物理 microbatch | 生成和训练均为 1 条回答 |
| 有效 batch | 每次 8 个 prompt，每题 8 条回答，共 64 条回答 |
| 长度 | prompt 2,048，回答 2,048，总长 4,096 |

LoRA 训练基座冻结。参考策略是加载时的 Qwen3-14B，通过临时关闭新加入的 LoRA 适配器计算参考 log-probability，再恢复适配器。每批 rollout 在 Actor 更新前缓存旧策略 log-probability，生成和 teacher-forced 概率计算保持相同的 dropout、输出支持集与采样温度。

生成与训练复用 Actor 权重并分阶段释放 KV cache。Actor 更新采用 activation checkpointing。RM 参数冻结仍需保存或重算输入反向所需的激活；接入 trainer 时使用支持冻结参数和输入梯度的 checkpoint 包装，并核对其实际生效。当前 `reward_input_gradients` 将 RM 设为 eval，HF 常规依赖 train 模式的 checkpoint 开关需要在适配层处理。

每次只计算一条回答的旧 logits，将其送到 GPU 1，与该回答的输入梯度和 RM embedding 完成 credit 运算，随后释放临时张量。完成全部 credit 缓存后才开始 Actor 更新。有效 batch 通过梯度累积实现。

14B BF16 权重约 28GB，8B 约 16GB。两卡方案是工程预算；实际峰值显存和吞吐由最终运行记录。

## 4. GRPO 与 VPO-RM 的连接

同一 prompt 的 8 条回答先完成评分，计算

$$
A_i=\frac{R_i-\bar R}{\sigma},\qquad
\sigma=\sqrt{\frac1G\sum_i(R_i-\bar R)^2}+10^{-6}.
$$

GRPO 使用 $\widetilde A_{it}=A_i$。VPO-RM 通过输入梯度构造

$$
f_t=\nabla_{e_t}R,\quad v_t=Wf_t,\quad
C_t=v_t-\langle p_t,v_t\rangle,
$$

$$
d_t=\frac{p_t(a_t)C_t(a_t)-\sum_b p_t(b)^2C_t(b)}{\sigma},
\qquad
\widetilde A_t=A\,T\operatorname{softmax}_t(Ad_t/\tau).
$$

当前固定 $\tau=1$。分配后 $\frac1T\sum_t\widetilde A_t=A$，两种方法共用组内统计、clipping、KL 和 loss reduction。token advantage、旧策略概率及参考策略概率均缓存并停止梯度。

VPO-RM 执行顺序：

1. Actor 硬采样回答，记录 token ID、mask 与 prompt 组 ID。
2. RM 逐回答执行前向和输入反向；奖励与回答位置梯度暂存 CPU。完整组评分后计算 $A_i,\sigma_i$。
3. Actor 在更新前逐回答执行 teacher-forced 前向；GPU 1 使用旧 logits、RM embedding 和缓存梯度构造 credit，再将最终缓存移到训练设备。
4. 关闭 LoRA 计算参考 log-probability，恢复 LoRA，按 microbatch 累积 policy loss 与 KL，执行一次优化器更新。

GRPO 使用同一 RM 的前向奖励，将序列 $A_i$ 扩展到有效回答位置，交给同一 loss 函数。

loss 先在每条回答内部按 token 平均，再对 64 条回答平均。KL 使用

$$
D_t=\exp(\log\pi_{\rm ref}(a_t)-\log\pi_\theta(a_t))
-\big(\log\pi_{\rm ref}(a_t)-\log\pi_\theta(a_t)\big)-1,
$$

最终最小化 clipped policy loss 加 $\beta$ 倍的同口径 KL 平均。

词表归约采用 FP32，token 块 128、词表块 8,192。代码利用等价展开累计 $\sum p_tv_t$、$\sum p_t^2$ 和 $\sum p_t^2v_t$。接口接收单条回答的完整 logits；分块限制 credit 临时张量，Actor 前向的完整 logits 仍计入显存。

## 5. 固定训练参数与结果记录

| 参数 | 数值 |
|---|---:|
| rollout 次数 | 500 |
| 每批 policy epoch | 1 |
| 每次优化器更新的回答数 | 64 |
| Actor 学习率 | $10^{-6}$ |
| 优化器 | AdamW |
| weight decay / 最大梯度范数 | 0.01 / 1 |
| clipping $\varepsilon$ | 0.2 |
| KL 系数 $\beta$ | 0.01 |
| 采样温度 / top-p / top-k | 1 / 1 / 0 |
| checkpoint 间隔 | 100 次 rollout |
| 训练种子 | 42 |

每次运行处理 4,000 次 prompt 采样、32,000 条回答，对应 500 次优化器更新。上述参数是统一的实验设计；机器可读配置描述实验，trainer 接入时应保存实际解析后的参数。

主表分别列出两个 RM 设置下 GRPO 与 VPO-RM 的指标和方法差值。对可逐题配对的指标按 prompt 重采样，报告 95% 置信区间；它反映固定训练结果下的评价样本不确定性。AlpacaEval 长度控制指标沿用官方统计实现。

学习曲线以累计生成 token 和 GPU-hours 为横轴，记录独立评价、RM 分数、KL、回答长度、截断率与策略熵。VPO-RM 额外记录权重集中度 $\mathrm{ESS}/T=T/\sum_t w_t^2$。效率表记录生成、RM 前向、RM 反向、credit 和 Actor 更新耗时，以及每张卡的峰值显存。GPU-hours 计入两张卡在训练期间的占用时间，评价成本单列。

## 6. 代码状态与使用

| 文件 | 功能 |
|---|---|
| [core.py](vpo_rm/core.py) | 组内 advantage、分块方向计算、乘法分配、GRPO policy loss |
| [reward.py](vpo_rm/reward.py) | 标量 RM pooling 与冻结 RM 输入反向 |
| [alignment.py](vpo_rm/alignment.py) | tokenizer、有效输出词表与位置映射检查 |
| [integration.py](vpo_rm/integration.py) | 旧 logits 提取、credit 缓存与 Actor loss |
| [check_models.py](check_models.py) | tokenizer 和配置检查 |
| [tests](tests) | 代理梯度、pooling、mask、缓存与参数更新检查 |

安装并运行软件测试：

```bash
cd VPO-RM
python -m pip install -e '.[hf,test]'
python -m pytest -q
python check_models.py --actor Qwen/Qwen3-14B \
  --rm project-themis/Themis-RM-14B --pad-token '<|endoftext|>'
python check_models.py --actor Qwen/Qwen3-14B \
  --rm Skywork/Skywork-Reward-V2-Qwen3-8B --pad-token '<|endoftext|>'
```

以上测试命令使用当前 Python 环境；登录节点的基础 `.venv` 不预装 PyTorch，
因此应在完成安装后运行，或直接在 rjob 目标容器中运行。正式训练命令会在容器
内显式安装 `torch`、Transformers、PEFT、Accelerate、PyArrow 和 Jinja2，不能把
登录节点的缺包状态当作 GPU 容器状态。

正式提交 Skywork 任务前，在目标容器中运行预检（硬限制为最多两张 GPU）：

```bash
python scripts/check_readiness.py --setting ultrafeedback_skywork_8b \
  --require-deps --require-data-reader --require-gpu \
  --json runs/readiness-skywork.json
```

预检会核对本地权重分片、配置和 tokenizer 哈希，确认 Skywork 的 padding
覆盖为 `<|endoftext|>`，检查 `train_prefs` parquet 的 prompt 列和可复现
split hash，并验证 trainer 入口及 CUDA 数量。登录节点缺少训练依赖时可省略
`--require-*` 仅查看静态检查结果；提交任务前必须在容器内以严格选项通过。

RM 接口为 `LastTokenReward(rm.base_model, rm.score)`。`vpo_rm/trainer.py` 和
`scripts/train_skywork.py` 提供 Skywork 设置的两卡 GRPO/VPO-RM 训练入口，包含
本地 parquet 数据加载、LoRA、KL、checkpoint、metrics 和 manifest；独立评价调度仍需
按评价平台配置单独执行。`experiments.json` 是实验规格，大模型训练结果将在实际运行后记录。
