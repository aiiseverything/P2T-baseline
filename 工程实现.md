# VPO-RM：实现与实验方案

VPO-RM 用冻结 RM 的输入梯度构造 token credit，再通过原 GRPO 的 clipped objective 更新 Actor。推导见 [数学原理](数学原理.md)。代码实现普通 softmax 的 ST 代理关系：

$$
f_t=\nabla_{e_t}R,\quad v_t=Wf_t,\quad
C_t=v_t-\langle p_t,v_t\rangle,\qquad
 d_t=\frac{p_t(a_t)C_t(a_t)-\sum_b p_t(b)^2C_t(b)}{\sigma},
$$

$$
\widetilde A_t=A\,T\operatorname{softmax}_t(Ad_t/\tau).
$$

## 1. 模型与 tokenizer

**当前方法需要 Actor 和 RM 使用相同的 tokenizer：分词规则、token 到 ID 的映射及特殊 token 的语义一致。** Actor 的 $p_t(b)$ 与 RM embedding 矩阵的第 $b$ 行直接相乘，因此相同词表大小只满足张量维度，还需要相同的 token 含义。Actor 与 RM 可以有不同的参数量、隐藏维度和 embedding 权重。

代码在加载后调用 `check_tokenizers`，比较完整词表、特殊 token 配置及 fast tokenizer 的分词定义。`shared_output_mask` 给出有效输出词表，覆盖已登记 token，并处理 padding 和模型为对齐预留的空余词表行。生成、旧策略概率和策略更新使用同一个输出支持集；在支持集上计算完整 softmax。

2026-09-08 已读取下列公开模型的 `tokenizer.json`、`tokenizer_config.json` 和 `config.json`，并用实际 tokenizer 对象检查对齐结果：

| 模型组合 | 核对结果与配置 |
|---|---|
| CleanRL Pythia TL;DR SFT / RM，1B 与 2.8B | 基础词表和分词规则一致；SFT 多登记了 `[PAD]`。在两侧统一登记为 ID 50277，设置为 padding，使用现有 50304 行词表；对齐检查通过 |
| Qwen3-8B / Skywork-Reward-V2-Qwen3-8B | `tokenizer.json` 一致；padding 默认值有差异。统一设置为 `<\|endoftext\|>`，保留显式 attention mask；对齐检查通过 |
| Qwen2.5-Math-7B-Instruct / Qwen2.5-Math-PRM-7B | 特殊 token 发生 ID 语义冲突，例如 151651 分别代表 `<\|quad_end\|>` 与 `<extra_0>`；配对需要额外的词表适配方案 |

Pythia 的配置方式：

```python
for tokenizer in (actor_tokenizer, rm_tokenizer):
    tokenizer.add_special_tokens({"pad_token": "[PAD]"})
assert actor_tokenizer.pad_token_id == rm_tokenizer.pad_token_id == 50277
```

Qwen3 的配置方式：

```python
for tokenizer in (actor_tokenizer, rm_tokenizer):
    tokenizer.pad_token = "<|endoftext|>"

check_tokenizers(
    actor_tokenizer, rm_tokenizer,
    actor.get_output_embeddings().weight.shape[0],
    rm.get_input_embeddings().weight.shape[0],
)
```

同步设置模型与 generation config 的 `pad_token_id`。这些操作调整 tokenizer 登记及 padding 设置，embedding 使用 checkpoint 已有的行。

RM 可以保留自己的 prompt 模板。生成后的回答 token ID 直接放进 RM 输入，并保存位置映射。Actor 预测 $a_t$ 的 logits 位于该 token 的前一个位置，RM 输入梯度位于 $a_t$ 自身的位置；`check_response_tokens` 核对映射后的 ID、attention mask 和顺序。

模型来源：[Pythia SFT](https://huggingface.co/cleanrl/EleutherAI_pythia-1b-deduped__sft__tldr)、[Pythia RM](https://huggingface.co/cleanrl/EleutherAI_pythia-1b-deduped__reward__tldr)、[Qwen3](https://huggingface.co/Qwen/Qwen3-8B)、[Skywork RM](https://huggingface.co/Skywork/Skywork-Reward-V2-Qwen3-8B)、[Qwen Math PRM](https://huggingface.co/Qwen/Qwen2.5-Math-PRM-7B)。

## 2. 代码结构与接入

| 文件 | 功能 |
|---|---|
| [core.py](vpo_rm/core.py) | 完整组内 advantage、分块方向计算、乘法分配、GRPO policy loss |
| [reward.py](vpo_rm/reward.py) | 冻结 RM 输入反向、显式末位置打分、PRM step 分数聚合接口 |
| [check_models.py](check_models.py) | 仅读取 tokenizer 与配置的模型配对检查命令 |
| [alignment.py](vpo_rm/alignment.py) | tokenizer 检查、有效输出词表、Actor / RM 位置映射 |
| [integration.py](vpo_rm/integration.py) | 旧策略 logits 提取、rollout credit 缓存、Actor policy loss |
| [tests](tests) | 显式 ST 对照、有限差分、pooling、padding 和 Actor 更新测试 |
| [experiments.json](configs/experiments.json) | 最终实验矩阵与训练参数 |

安装与软件测试：

```bash
cd VPO-RM
python -m pip install -e '.[hf,test]'
python -m pytest -q
```

本地使用 PyTorch 2.14.0、Transformers 5.16.1 在 CPU 上完成 15 项软件测试，覆盖解析梯度、词表屏蔽、RM 接口及 Actor 参数更新。完整模型训练由以下实验方案安排。

代码作为已有 GRPO trainer 的算法模块使用，数据读取、rollout 调度、参考模型 KL、checkpoint 和分布式训练由 trainer 管理。`LastTokenReward` 适配 GPTNeoX / Qwen 的 decoder backbone 与标量 `score` head：

```python
from vpo_rm import LastTokenReward
reward_model = LastTokenReward(rm.base_model, rm.score)
```

打分显式选择最后有效位置，也可传入 checkpoint 所需的 `score_positions`。这让 `inputs_embeds` 前向与硬 ID 前向使用相同 pooling。`MeanStepReward` 接受二分类 PRM 的逐位置 logits，在 `step_mask` 处平均正类概率；最终实验使用下文列出的标量 RM。

每批 rollout 的连接顺序：

```python
from vpo_rm import (
    actor_response_logits, response_reward_gradients, group_advantages,
    build_credit_cache, actor_policy_loss, shared_output_mask,
)

output_mask = shared_output_mask(
    actor_tokenizer, actor.get_output_embeddings().weight.shape[0], device
)
# rollout generation 使用 temperature=1、top_p=1、top_k=0，并同步：
# suppress_tokens=(~output_mask).nonzero().flatten().tolist()

rewards, f = response_reward_gradients(
    reward_model, rm_ids, rm_mask, rm_positions, token_ids, response_mask,
)
# group_ids 指向完整 prompt 组。跨设备时先汇总奖励，再取回对应 A、sigma。
A, sigma = group_advantages(rewards, group_ids)

old_actor.eval()
with torch.no_grad():
    old_logits = actor_response_logits(
        old_actor, actor_ids, actor_mask, actor_positions, response_mask,
        output_mask=output_mask,
    )
cache = build_credit_cache(
    old_logits, token_ids, f, reward_model.get_input_embeddings().weight,
    A, sigma, response_mask, tau=tau,
    token_chunk_size=128, vocab_chunk_size=8192,
)
del old_logits, f

optimizer.zero_grad()
policy_loss = actor_policy_loss(
    actor, actor_ids, actor_mask, actor_positions, token_ids,
    response_mask, cache, output_mask=output_mask,
)
loss = policy_loss + beta * baseline_kl_loss
loss.backward()
optimizer.step()
```

示例变量来自 trainer 的一批 rollout。`actor_positions` / `rm_positions` 是实际回答 token 的位置，形状为 $B\times T$；无效位置可填 $-1$。`token_ids` 与 `response_mask` 同形。EOS 参与 loss，prompt 和 padding 由 mask 排除。旧策略与更新策略使用一致的 dropout 设置；credit 在同一批 rollout 的多轮更新中保持缓存值。

所有 credit 输入放在同一设备。RM 参数冻结，输入反向穿过完整网络；`reward_input_gradients` 支持外层 `no_grad` / `inference_mode`，对 batch 分数求和保留每条回答自身的 reward 尺度。

## 3. 数值尺度与计算成本

组内标准差采用总体标准差加 $\epsilon=10^{-6}$，与当前数学定义一致；对照 GRPO 使用相同统计口径。分配后

$$
\frac1T\sum_t\widetilde A_t=A.
$$

loss 按回答内部 token 平均，再按回答平均。基线的 KL estimator 与 reduction 直接复用；缓存的 old log-probability 和 token advantage 停止梯度。

分块计算使用以下等价式，令 $\mu_t=\sum_b p_t(b)v_{t,b}$：

$$
\sigma d_t
=p_t(a_t)(v_{t,a_t}-\mu_t)
-\sum_b p_t(b)^2v_{t,b}
+\mu_t\sum_b p_t(b)^2.
$$

代码累计 $\mu_t$、$\sum p_t^2$ 和 $\sum p_t^2v_t$，以 FP32 完成归约。中间词表张量受 token 块和词表块限制。调用接口仍接收完整旧 logits 和完整 RM embedding 矩阵；词表分片的训练框架需要在适配层增加跨 rank 归约。

额外成本主要是一次 RM 输入反向、旧 Actor 的 teacher-forced 前向，以及词表方向乘法。RM 激活在 credit 计算后释放。实验记录生成、RM 前向、RM 反向、credit 和 Actor 更新各自的耗时、峰值显存及总 GPU-hours。

## 4. 文献调研与实验选择

| 工作 | 原文采用的实验 | 对本研究的启发 |
|---|---|---|
| [TL;DR 复现](https://arxiv.org/html/2403.17031v1) | Pythia 1B、2.8B、6.9B，摘要偏好训练，公开 SFT / RM | 摘要适合检验可微偏好奖励，并支持模型规模比较 |
| [GRADE](https://arxiv.org/html/2601.11574v1#S6) | IMDB 情感控制生成，比较直接奖励反传与策略梯度 | 在摘要上比较直接 ST 更新与 GRPO credit 更新，更贴近本研究目标 |
| [P2T](https://openaccess.thecvf.com/content/CVPR2026/html/Wu_Unlocking_Token_Rewards_via_Training-Free_Reward_Attribution_CVPR_2026_paper.html) | 数学、代码和多模态推理；输入梯度提供 token 奖励 | 需要同 RM 的梯度归因比较，并报告独立任务指标 |
| [DelTA](https://arxiv.org/html/2605.21467v1#S4) | Qwen3-8B / 14B，DeepMath-103K，数学评测 | 局部方向与正则化分配已有相关工作，比较不同方向来源具有价值 |
| [OAR](https://arxiv.org/html/2601.07408v1#S4)、[GRAIL](https://arxiv.org/html/2606.04889v1#S4) | 数学推理，outcome 敏感性或 Actor 梯度显著性重加权 | 数学实验应覆盖这些相近的乘法 advantage 方法 |

**最终实验采用三个任务、四个设置：TL;DR 的两个规模，通用指令偏好，以及数学推理。** TL;DR 回答偏好优化问题；指令任务覆盖更开放的生成；数学任务用答案正确率衡量 RM 引导是否转化为实际能力。数学与指令复用对齐的 Qwen3 / Skywork 组合，分别独立训练。

## 5. 最终实验矩阵

| 设置 | Actor / 冻结 RM | 训练数据 | 最终评测 |
|---|---|---|---|
| TL;DR-1B | `cleanrl/EleutherAI_pythia-1b-deduped__sft__tldr` / 同前缀 `__reward__tldr` | TL;DR train prompts | 摘要偏好、事实一致性、ROUGE-L、长度 |
| TL;DR-2.8B | `cleanrl/EleutherAI_pythia-2.8b-deduped__sft__tldr` / 同前缀 `__reward__tldr` | 同一份 TL;DR prompts | 同上；同时比较规模变化 |
| Instruction-8B | `Qwen/Qwen3-8B` / `Skywork/Skywork-Reward-V2-Qwen3-8B` | UltraFeedback 的训练 prompts | AlpacaEval 2 的 length-controlled win rate、IFEval strict accuracy |
| Math-8B | 同上 | MATH 训练集 | MATH-500、AIME24、AIME25、AMC23、MinervaMath 的平均单次正确率 |

数据来源：[TL;DR](https://huggingface.co/datasets/CarperAI/openai_summarize_tldr)、[UltraFeedback](https://huggingface.co/datasets/HuggingFaceH4/ultrafeedback_binarized)、[MATH](https://huggingface.co/datasets/EleutherAI/hendrycks_math)。UltraFeedback 使用 `train_prefs` 中去重后的 prompt；TL;DR 使用公开 train / valid / test 分割，保留原 prompt 格式。指令和数学均设置 `enable_thinking=False`；数学 prompt 明确要求展示解题过程并以 boxed answer 结束，因此被 RM 评分的回答仍包含推导文本。

数学主比较中，$R_i$ 使用 Skywork 的序列分数，GRPO 与 VPO-RM 共用同一 $R_i$。正确率由独立的 [Math-Verify](https://github.com/huggingface/Math-Verify) 评估。这样主表的差别对应 credit 分配；Skywork 在数学分布上的奖励质量则通过独立正确率反映。全体回答 token 进入 RM，保留推导到 reward 的梯度路径。

## 6. 对照方法

所有设置均比较 SFT / 初始 Actor、原始 GRPO、P2T 的同奖励适配和 VPO-RM。初始 Actor 是推理参照；其余方法从相同 checkpoint 独立训练。

P2T 原文第 3.3 节使用 coarse reward 构造 token 奖励，再加到 outcome advantage。这里将当前同一标量 $R_i$ 作为 coarse reward，把整条回答视为一个评分片段，复现其输入替换归因和加法更新，表中明确标为 **P2T（同 RM 适配）**。其公式保持为

$$
I_{it}=f_{it}^{\top}(e_{it}-e_{\mathrm{null}}),\quad
R^{\mathrm{P2T}}_{it}=R_i+\omega R_i\operatorname{softmax}_t(I_{it}),\quad
\widetilde A^{\mathrm{P2T}}_{it}=A_i+\alpha R^{\mathrm{P2T}}_{it}.
$$

使用 padding embedding 作为 null，$\omega=0.6$，$\alpha$ 在验证集选择。此处保留原式的加法结构；总量按公式计算，$\sum_tR^{\mathrm{P2T}}_{it}=(T_i+\omega)R_i$。[P2T 原文](https://openaccess.thecvf.com/content/CVPR2026/papers/Wu_Unlocking_Token_Rewards_via_Training-Free_Reward_Attribution_CVPR_2026_paper.pdf)

另外安排以下完整训练对照：

| 设置 | 方法 | 比较问题 |
|---|---|---|
| TL;DR-1B | GRADE-STE，使用同 SFT / RM 与作者的 Gumbel-ST 生成路径 | 直接奖励反传与 GRPO credit 更新的质量、KL 和计算成本 |
| Math-8B | GRAIL、OAR-G、DelTA 的同 GRPO / 同 RM 适配 | Actor 显著性、outcome 敏感性、组内梯度方向与 RM 方向信号的差别 |

后三者保留各自 credit 构造方式，统一接入本实验的组内标准化、clipping 和回答平均 loss；表名标明“同 GRPO 适配”。GRAIL / OAR-G 使用回答中的 final-answer span；DelTA 使用 $A_i$ 的正负划分。它们的原文 RLVR 结果作为相关研究背景，当前表格报告实际重训结果。

每个设置、每种训练方法运行种子 13、42、87。主比较共 48 次训练：四个设置的 GRPO / P2T / VPO-RM 共 36 次，GRADE 3 次，数学的另外三种方法共 9 次。

## 7. 训练与评测协议

以下数值是本研究的实验设计，机器可读版本见 [experiments.json](configs/experiments.json)。

| 参数 | TL;DR | Instruction | Math |
|---|---:|---:|---:|
| 每次 rollout 的 prompt 数 | 32 | 32 | 32 |
| 每 prompt 回答数 $G$ | 8 | 8 | 8 |
| rollout 次数 | 1000 | 1000 | 1000 |
| prompt 最大 token 数 | 1024 | 2048 | 2048 |
| 回答最大 token 数 | 128 | 2048 | 4096 |
| Actor 学习率 | $3\times10^{-6}$ | $10^{-6}$ | $10^{-6}$ |
| clipping $\varepsilon$ | 0.2 | 0.2 | 0.2 |
| KL 系数 $\beta$ | 0.05 | 0.01 | 0.01 |

采用 AdamW、weight decay 0.01、梯度范数上限 1、BF16 模型计算。每批 rollout 完成一次完整 policy epoch，optimizer minibatch 为 64 条回答，通过梯度累积实现。训练采样温度 1、top-p 1、top-k 0；有效输出词表与所有概率计算一致。每 100 次 rollout 保存 checkpoint。Actor 采用全参数训练，RM 与参考 Actor 冻结。所有方法保留相同数据顺序、长度上限和单批生成数量；各自策略产生各自的回答。

GRADE 按相同 prompt / 回答预算组织训练，优化器更新与生成路径按其方法实现，另报实际消耗。其余方法统一 GRPO trainer；分布式实现采用数据并行或参数分片，并在 prompt 组内汇总奖励。正式报告记录实际 GPU 型号、数量、软件版本和吞吐。

**超参数选择。** VPO-RM 的 $\tau$ 搜索 $\{10^{-3},10^{-2},10^{-1},1,10\}$；P2T 的 $\alpha$ 搜索 $\{0.01,0.03,0.1,0.3,1\}$。每个候选按完整训练预算运行，以独立验证集表现选择，然后运行三个报告种子。其他方法使用作者设置，并给予相同数量的完整训练候选预算。搜索计算量单独计入成本；报告全部候选结果与所选参数。

TL;DR 使用 valid 选择参数，test 中固定抽取 2000 个 prompt 进行最终配对评价。指令从训练 prompts 按 prompt hash 留出 2000 条验证样本；数学从 MATH train 按学科分层留出 500 题。摘要和指令的最终生成采用温度 0.7、top-p 0.9，每 prompt 一条回答，长度上限与训练相同。指令验证集使用同一独立 judge 评价相对初始 Actor 的配对胜率。数据处理时按规范化题目文本及公开重叠清单去重，并保存最终 split ID。最终主表取预定训练终点 checkpoint，同时展示完整学习曲线。

**质量评价。**

- TL;DR：对 VPO-RM 与各对照在同一 prompt 上的摘要盲评，交换答案顺序复评，平局计 0.5；同时比较人工参考摘要。采用 [Qwen2.5-72B-Instruct](https://huggingface.co/Qwen/Qwen2.5-72B-Instruct) 作为独立 judge，固定模型 revision、温度 0 和评分模板，统一评价事实忠实度、信息覆盖和简洁性；固定抽取 300 个 prompt，由三名标注者进行人工复核。ROUGE-L 和 RM 分数作为辅助指标。
- Instruction：使用 [AlpacaEval 2 官方评测](https://github.com/tatsu-lab/alpaca_eval) 的 length-controlled win rate 与 [IFEval](https://github.com/google-research/google-research/tree/master/instruction_following_eval) 的 strict prompt / instruction accuracy。报告 judge 的准确模型版本、评测模板和生成配置。
- Math：每题采样 16 个回答，温度 0.6、top-p 0.95，长度上限与训练一致。报告 $\frac1N\sum_q\frac1{16}\sum_k\mathbf1\{\hat a_{qk}=a_q\}$，即平均单次正确率；逐数据集列出结果，并给出五个数据集的宏平均。报告解析失败率与长度截断率。

三种任务都报告三个训练种子的均值与标准差；方法差值的 95% 置信区间采用按种子、prompt 两层重采样的 paired bootstrap。数学题内的 16 次采样随题一起重采样。

## 8. 消融、效率与最终图表

消融在 TL;DR-2.8B 和 Math-8B 上按相同训练预算、三个种子完成：

| 变体 | 改动 | 要回答的问题 |
|---|---|---|
| 梯度强度分配 | 用 $|A|\|g_t\|_2$ 替换效用 $Ad_t$ | 有符号的策略方向响应是否比梯度大小更有效 |
| 省略 advantage 方向调节 | 用 $d_t$ 替换效用 $Ad_t$，最终系数仍为 $Aw_t$ | 正负 advantage 对分配方向的调节是否重要 |

两项消融共 12 次完整训练。$\tau$ 的完整搜索结果构成分配强度敏感性图；原始 GRPO 同时提供 $w_t=1$ 的参照。

效率曲线分别以累计生成 token 数和累计 GPU-hours 为横轴，质量指标为纵轴。除相同 rollout 预算的终点结果外，再将各方法训练到同设置 GRPO 的总 GPU-hour 预算，报告对应的质量；这一轮运行作为独立的等算力结果记录。成本包含旧策略前向、RM 反向和 credit 计算。

论文呈现四组结果：

1. **主结果表：** 摘要、指令、数学的独立质量指标，以及相对原始 GRPO 的差值和置信区间。
2. **效率图：** 质量—生成 token、质量—GPU-hours；配套阶段耗时与显存表。
3. **消融表与温度图：** 完整方向、梯度强度、advantage 方向调节，以及 $\tau$ 对质量与权重集中度的影响。
4. **训练行为图：** RM 分数与独立质量、KL、回答长度、熵，以及 $\mathrm{ESS}/T=T/\sum_tw_t^2$ 随训练变化；按 $A>0$ 和 $A<0$ 分组展示权重。

这些实验共同评价 RM 方向信息的有效性、乘法分配的作用，以及新增反向计算是否带来实际质量收益。
