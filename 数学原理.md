# 基于 RM 梯度的 GRPO 乘法 Credit

利用冻结 Reward Model 的输入梯度，评价各 token 的策略更新对奖励的局部影响，再将序列 advantage 分配为 token advantage。Actor 与 RM 使用对齐的 tokenizer，策略训练采用 GRPO。

RM 在本方案中是一个整体可微评分模型。数学主线为：

$$
\text{硬采样与 ST 代理反向}
\longrightarrow\text{端到端梯度展开}
\longrightarrow\text{token 更新方向评价}
\longrightarrow\text{乘法 advantage}.
$$

## 1. 目标与符号

对 prompt $x$，Actor 采样回答 $y=(a_1,\ldots,a_T)$。位置 $t$ 的前缀、logits 和概率为

$$
s_t=(x,a_{<t}),\qquad
\ell_t=\ell_\theta(s_t),\qquad
p_t=\operatorname{softmax}(\ell_t).
$$

概率定义在 Actor 与 RM 对齐的有效输出词表上；固定屏蔽的 padding 和预留词表行取概率零，生成与更新共用该支持集。

令 $u_t$ 为 $a_t$ 的 one-hot，RM embedding 矩阵为 $W\in\mathbb R^{V\times d}$，对应输入为 $e_t=W^\top u_t$。RM 记为 $R_\phi(x,E)$，参数 $\phi$ 包含 $W$，在策略训练期间冻结。

原始离散期望奖励及其策略梯度为

$$
\begin{aligned}
J_{\mathrm{RL}}(\theta)
&=\mathbb E_{y\sim\pi_\theta}[R_\phi(x,y)],\\
\nabla_\theta J_{\mathrm{RL}}
&=\sum_y R_\phi(x,y)\nabla_\theta\pi_\theta(y\mid x)\\
&=\mathbb E_y\left[R_\phi(x,y)
\sum_t\nabla_\theta\log\pi_\theta(a_t\mid s_t)\right].
\end{aligned}
$$

后文在一条已采样回答上固定硬前缀，构造 RM 的代理输入梯度，以此指导 GRPO 更新。

## 2. 硬采样与概率代理反向

定义概率加权 embedding 与 ST 输入：

$$
\bar e_t = W^\top p_t, \qquad
\widetilde e_t = \bar e_t + \operatorname{sg}(e_t - \bar e_t),
$$

其中 $\operatorname{sg}$ 表示 stop-gradient。其前向值与代理反向分别为

$$
\widetilde e_t=e_t,
\qquad
\frac{\widehat\partial\widetilde e_t}{\partial p_t}=W^\top.
$$

因此 RM 前向处理实际采样 token 的 embedding，反向使用概率输入的 Jacobian。当前定义采用普通 softmax 概率 $p_t$；ST Gumbel-Softmax 则使用带 Gumbel 噪声和温度的软概率，两者对应各自的代理 Jacobian。[ST Gumbel-Softmax](https://arxiv.org/abs/1611.01144)

这里的 ST 是采样处指定的代理导数。固定采样噪声负责确定前向样本，ST 负责给出可用的输入反向路径。

## 3. 从端到端求导展开到策略梯度形式

对概率加权输入使用乘积法则：

$$
d\bar e_t=W^\top dp_t+(dW^\top)p_t.
$$

沿 Actor 参数变化时，RM 参数固定，$dW=0$，留下 $d\bar e_t=W^\top dp_t$。冻结消除的是参数变化项，RM 对输入的 Jacobian 仍参与反向。令

$$
f_t=\left.\nabla_{\widetilde e_t}R_\phi(x,\widetilde E)
\right|_{\widetilde E=E},
\qquad v_t=Wf_t.
$$

通过 ST 代理路径求导：

$$
\begin{aligned}
\widehat\nabla_\theta R
&=\sum_t\left(\frac{\partial p_t}{\partial\theta}\right)^\top Wf_t\\
&=\sum_t\sum_bv_{t,b}\nabla_\theta p_t(b)\\
&=\sum_t\sum_bp_t(b)v_{t,b}
\nabla_\theta\log\pi_\theta(b\mid s_t).
\end{aligned}
$$

将词表系数中心化：

$$
\bar v_t=\sum_bp_t(b)v_{t,b},\qquad
C_t(b)=v_{t,b}-\bar v_t.
$$

利用概率和恒等式，

$$
\sum_bp_t(b)\bar v_t\nabla_\theta\log p_t(b)
=\bar v_t\nabla_\theta\sum_bp_t(b)=0,
$$

得到

$$
\boxed{
\widehat\nabla_\theta R
=\sum_t\sum_bp_t(b)C_t(b)
\nabla_\theta\log\pi_\theta(b\mid s_t),
\qquad\sum_bp_t(b)C_t(b)=0.
}
$$

这保留了早期方法的核心：端到端求导自然产生 REINFORCE 形式的中心化系数。帽号表示上述等式对应指定的 ST 代理反向；$f_t$ 和整组 $C_t(b)$ 在同一条硬采样回答处计算。

### 概率与 advantage 的位置

固定本次硬回答 $y$，用 $C_t^y$ 标明系数在该回答处计算。上式可写为

$$
\widehat\nabla_\theta R(y)
=\sum_t\mathbb E_{b\sim p_t}
\left[C_t^y(b)\nabla_\theta\log p_t(b)\right].
$$

这里 $p_t(b)$ 是期望的概率权重，$C_t^y(b)$ 是中心化的类 advantage 系数。若把 $D_t(b)=p_t(b)C_t^y(b)$ 定义为整个求和系数，则相同表达为 $\sum_bD_t(b)\nabla\log p_t(b)$；改写成概率期望时，系数相应为 $D_t(b)/p_t(b)=C_t^y(b)$。

这个期望只是在固定 $y$ 后重写词表求和。$C_t^y$ 依赖整条硬回答，直接用产生该回答的同一个 $a_t$ 替代词表求和，需要另行验证其估计偏差。当前工程通过完整词表运算计算方向信号。

训练阶段使用的 token advantage 则是第 5 节的 $\widetilde A_t=A w_t$。$C_t$、$d_t$ 和 $\widetilde A_t$ 分别承担代理梯度系数、方向评价和策略更新系数的作用。

为得到工程所需的 logit 梯度，继续展开 softmax：

$$
B_t=\frac{\partial p_t}{\partial\ell_t}
=\operatorname{diag}(p_t)-p_tp_t^\top,
$$

$$
\boxed{
\widehat\nabla_{\ell_t}R=B_tv_t=p_t\odot C_t.
}
$$

## 4. 与 GRPO 的 token 更新方向连接

同一 prompt 下采样 $G$ 条回答，使用原 GRPO 的组内奖励统计：

$$
b=\frac1G\sum_iR_i,\qquad
\sigma=\sqrt{\frac1G\sum_i(R_i-b)^2}+\epsilon,\qquad
A_i=\frac{R_i-b}{\sigma}.
$$

以下省略回答下标 $i$。构造 credit 时将缓存的 $b,\sigma$ 视为常量。定义标准化奖励梯度

$$
g_t=\frac1\sigma\widehat\nabla_{\ell_t}R.
$$

已采样 token 的对数概率展开为

$$
\log p_t(a_t)=\ell_{t,a_t}-\log\sum_b\exp(\ell_{t,b}),
$$

故其 score 向量为

$$
\boxed{k_t:=\nabla_{\ell_t}\log p_t(a_t)=u_t-p_t.}
$$

GRPO 的重要性比率为 $\rho_t=\pi_\theta(a_t\mid s_t)/\pi_{\mathrm{old}}(a_t\mid s_t)$。在 $\theta=\theta_{\mathrm{old}}$ 处，$\rho_t=1$，clipping 局部处于线性区间：

$$
\nabla_{\ell_t}J_{\mathrm{policy}}
=\frac AT\nabla_{\ell_t}\rho_t
=\frac AT\rho_t k_t
=\frac ATk_t.
$$

将各位置 logits 视为独立坐标，原 GRPO 的局部上升方向为 $\delta\ell_t=\eta Ak_t/T$。据此定义方向评价

$$
\begin{aligned}
d_t
&=\sum_b g_{t,b}k_{t,b}\\
&=\frac1\sigma\sum_b p_t(b)C_t(b)
\left[\mathbf 1\{b=a_t\}-p_t(b)\right]\\
&=\frac1\sigma\left[
\sum_b p_t(b)C_t(b)\mathbf 1\{b=a_t\}
-\sum_b p_t(b)^2C_t(b)\right]\\
&=\boxed{\frac1\sigma\left[p_t(a_t)C_t(a_t)
-\sum_b p_t(b)^2C_t(b)\right]}.
\end{aligned}
$$

第一项中的指示函数只留下已采样 token；第二项的平方来自 $g_{t,b}$ 中的 $p_t(b)$ 与 $k_{t,b}$ 中的 $-p_t(b)$ 相乘。中心化恒等式是 $\sum_b p_t(b)C_t(b)=0$，带平方的求和通常仍有非零值。

例如 $p_t=(0.2,0.5,0.3)$、$C_t=(1,-1,1)$、$a_t=2$、$\sigma=1$，则

$$
g_t=(0.2,-0.5,0.3),\quad k_t=(-0.2,0.5,-0.3),
\qquad d_t=-0.38=-0.5-(0.04-0.25+0.09).
$$

$d_t$ 衡量沿提高已采样 token 概率的梯度方向移动时，标准化奖励的预计变化。GRPO 的实际正负方向由 $A$ 决定，因此分配使用的效用为 $Ad_t$。

## 5. 从更新预算导出乘法分配

令 $q_t\ge0$、$\sum_tq_t=1$，把回答的更新系数分配给各位置：

$$
\delta\ell_t(q)=\eta Aq_tk_t.
$$

按代理梯度作一阶展开：

$$
\widehat{\Delta(R/\sigma)}
=\sum_tg_t^\top\delta\ell_t(q)
=\eta A\sum_tq_td_t.
$$

原 GRPO 对应 $q_t^0=1/T$。通过 KL 正则控制分配相对原系数的偏离：

$$
q^*=\arg\max_{q\in\Delta_T}
\left\{A\sum_tq_td_t-\tau D_{\mathrm{KL}}(q\|q^0)\right\},
\qquad\tau>0.
$$

学习率吸收到正则参数中。奖励增量来自上述一阶展开，KL 是分配的稳定化选择。拉格朗日一阶条件为

$$
Ad_t-\tau\big(\log(Tq_t)+1\big)+\lambda=0.
$$

所以

$$
q_t^*=\frac{\exp(Ad_t/\tau)}{\sum_j\exp(Ad_j/\tau)}.
$$

记分配目标为 $F(q)$。对上述解有

$$
F(q^*)-F(q)=\tau D_{\mathrm{KL}}(q\|q^*)\ge0,
$$

因此 $q^*$ 是该正则化分配问题的唯一最优解。

保留 GRPO loss 的 token 平均 $1/T$，得到最终系数

$$
\boxed{
\widetilde A_t=A\,w_t,
\qquad
w_t=T\frac{\exp(Ad_t/\tau)}{\sum_j\exp(Ad_j/\tau)}.
}
$$

$A>0$ 时，权重偏向较大的 $d_t$；$A<0$ 时，权重偏向较小的 $d_t$，因为降低这些 token 的概率具有更高的预计收益。

## 6. 性质与实际训练目标

权重满足

$$
w_t>0,\qquad\frac1T\sum_tw_t=1,\qquad
\boxed{\frac1T\sum_t\widetilde A_t=A.}
$$

因此 token advantage 保持序列 advantage 的符号和平均值。$A=0$ 时 policy 系数全部为零；各 $d_t$ 相同或 $\tau\to\infty$ 时恢复原 GRPO 系数。

由 $q^0$ 的可行性和 KL 非负性，

$$
A\sum_tq_t^*d_t
-\tau D_{\mathrm{KL}}(q^*\|q^0)
\ge\frac AT\sum_td_t,
$$

$$
\boxed{A\sum_tq_t^*d_t\ge\frac AT\sum_td_t.}
$$

这证明：在所选代理梯度和局部 logit 更新模型下，分配后的预计一阶奖励增量至少达到原分配的预计增量。

策略训练使用缓存并停止梯度的 $\widetilde A_{it}$：

$$
J_{\mathrm{policy}}(\theta)
=\mathbb E_{\{y_i\}\sim\pi_{\mathrm{old}}}
\left[\frac1G\sum_i\frac1{T_i}\sum_t
\min\left(\rho_{it}\widetilde A_{it},
\operatorname{clip}(\rho_{it},1-\varepsilon,1+\varepsilon)
\widetilde A_{it}\right)\right].
$$

最终目标为 $J_{\mathrm{policy}}-\beta J_{\mathrm{KL}}$，KL 的定义和 reduction 沿用原 GRPO 基线。

### 等式与局部结论的适用范围

**代理梯度。** ST 指定了采样处的反向规则。一个可直接核对的例子是 $a\sim\operatorname{Bernoulli}(p)$、$p=\operatorname{sigmoid}(z)$、硬输入 $e=a$、奖励 $R(e)=e^2$：

$$
\frac{d}{dz}\mathbb E[R(a)]=p(1-p),\qquad
\widehat{\frac{\partial R}{\partial z}}=2a\,p(1-p),\qquad
\mathbb E\left[\widehat{\frac{\partial R}{\partial z}}\right]=2p^2(1-p).
$$

两者一般不同。固定采样噪声后，硬阈值映射对 logits 的普通导数几乎处处为零；可用的导数来自 ST 的代理定义。这里得到的是代理梯度的 REINFORCE 形式，以及据此设计的 credit。

**更新空间。** 第 4—5 节将各位置 logits 作为独立优化变量。若记 $L_t=\partial\ell_t/\partial\theta$，实际 Actor 在 rollout 策略处的纯 policy 梯度步满足

$$
\Delta\theta=\eta A\sum_u q_uL_u^\top k_u,
\qquad
\Delta\ell_t=\eta A\sum_u q_uL_tL_u^\top k_u+O(\eta^2).
$$

共享参数引入跨位置项与 $L_tL_u^\top$ 的几何作用。因此上述最优性结论对应独立 logit 的局部模型；实际参数更新的效果需要结合优化器、KL 和 clipping 验证。系数均值保持对应 token 平均尺度，参数梯度范数仍随分配变化。

实现接口与实验方案见 [工程实现](工程实现.md)。
