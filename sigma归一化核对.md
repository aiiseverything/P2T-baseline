# σ 归一化核对:作者的归一化存在,但只做了一半

> 目的:回应"数学推导的作者说他做了 RM 归一化"这个说法——核对文档和代码,**说法属实,但不影响 τ 失配的诊断结论**。
> 前置阅读:[tau失配与修复方案.md](tau失配与修复方案.md)(问题背景、ESS 指标、方案 B 的完整说明)。

---

## 一句话结论

作者确实做了归一化(RM 梯度除以组内奖励标准差 σ,文档和代码都有);**它解决的是"单位换算"问题,没解决"数值大小"问题**。softmax 需要的是后者。实测归一化之后的量离要求还差 20~100 倍(ESS 0.998),所以 τ 失配的结论不变。方案 B 不是对这个归一化的否定,而是它的**完备化**——σ 在 B 里会被精确抵消,原有的稳健性一点没丢。

## 1. 作者做的归一化:证据齐全

| 位置 | 内容 |
|---|---|
| `数学原理.md` §4(158–161 行) | 定义"标准化奖励梯度":RM 的 logit 梯度**除以 σ**(组内奖励标准差) |
| `工程实现.md`(92 行) | d_t 的公式里带 /σ |
| `vpo_rm/core.py`(126 行) | `direction = (...) / reward_scale[r]`,逐响应除以所在组的 σ |

另外两个相关细节:

- `vpo_rm/reward.py` 的输入梯度是裸 autograd,注释明确写了 "no batch rescaling"——链路上没有其他缩放;
- 同文件还有一句作者留的注释:"Check checkpoint-specific reward normalization separately"——说明作者也意识到 RM 分数尺度要单独检查,但没有在 credit 链路上做。

**这条核对项我们补查过了**:Skywork RM 的输出没有内置 sigmoid 或标准化(实测分数 0.77~10.8,明显是裸 logit 头),所以不存在"RM 自带归一化被漏掉"的情况,量纲问题就在 credit 链路上。

## 2. 为什么这个归一化只解决了一半

作者的推理链(推测):A 经过组归一化是 O(1) → d 除以 σ 之后也换算成了 advantage 单位 → 所以 A·d 在 τ=1 下应该也是 O(1)。

破绽:**把单位搞对 ≠ 把数值大小搞对。** σ 归一化和 softmax 的需求,是两根正交的轴:

| | σ 归一化回答的问题 | softmax 需要的问题 |
|---|---|---|
| 轴 | 奖励轴(回答**之间**) | 参数轴(回答**内部**) |
| 问的是 | "1 分 RM 分差,值多少个 advantage?" | "把一个 token 的 logit 抬高 1,能买多少个 advantage?" |
| 谁决定 | σ(组内分差,已知) | RM 在 embedding 空间的梯度陡峭程度(**自由参数,无人约束**) |

d 的大小由后者决定,而单个位置的 RM 输入梯度天然只有千分位量级(总分数的变化摊在几百个位置、几千个维度上)。除以 σ≈3.7 之后,d 依然是 0.001~0.01 量级。**σ 管横向,softmax 要纵向,归一化一根轴救不了另一根。**

## 3. 具体例子:同一批分数,三种处理

假设一条回答有 3 个 token,RM 梯度算出的贡献分是:

```
d = [0.0010, 0.0012, 0.0008]
      ↑第三个最没用    ↑第二个最有用(比第一个高 20%)
```

信息本身很清楚:三个 token 拉开约 ±20% 的差距,第二个最重要。现在看 softmax 能不能"看见"这个差距。

### 第一步:作者归一化之后(÷ 组内 σ = 3.7)

> **σ 和 3.7 的出处**:σ 的定义在 `core.py` 的 `group_advantages`——同一 prompt 采样 8 条回答,RM 打 8 个分,σ 就是这 8 个分的标准差(逐 prompt 组计算,组内 8 条共享;同时用于算 A = (R−均值)/σ 和除进 d_t)。RM 分数范围 0~12、组内能拉开数分,所以 σ 天然是"几分"量级。3.7 取自 p9c `metrics.jsonl` 中 `reward_std` 字段 191 个 rollout 的实测均值(3.683);该字段记录的是 rollout 内 64 条回答的混合 std,因 prompt 连号同源聚簇、组间差异小,可作组内 σ 的近似。结论对该值不敏感:σ 取 1~10 中任何值,d/σ 后仍远达不到 O(1) 离散度。逐组 σ 的精确统计将在 p9d 加入 metrics 记录。

```
d/σ = [0.00027, 0.00032, 0.00022]     ← 本来就小,又缩小了 3.7 倍
```

进 softmax(取 A=1、τ=1),权重:

```
q = [0.33334, 0.33336, 0.33333]       ← 三份蛋糕:1/3, 1/3, 1/3
ESS = 1.0000(完全平分,机制没启动)
```

**为什么明明有 20% 的差距,softmax 却看不见?** 因为 softmax 只看**差值**,不看**比值**:

- 它眼里只有 `0.00032 − 0.00027 = 0.00005` 这样的减法差;
- `exp(0.00005) ≈ 1.00005`,三个权重相差在万分之一量级 → 平分;
- 而"0.0012 是 0.0008 的 1.5 倍"这个**比值信息**,softmax 根本不接收。

作者的归一化是**等比缩放**(统一除以 σ):它保住了比值,却把差值除得更小了。它瞄准的是单位换算,顺手把本来就需要被放大的差值又缩了一遍——所以问题不但没解决,还略微加重了。

### 第二步:方案 B(逐回答标准化:减均值、除以标准差)

```
d = [0.0010, 0.0012, 0.0008]
均值 = 0.0010,标准差 = 0.000163
标准化后 = [0, +1.22, −1.22]           ← 差距被拉回到 O(1)
```

进 softmax(τ'=1):

```
q = [0.21, 0.72, 0.06]                ← 第二个 token 拿 72% 的学习信号
ESS = 0.58                            ← 落在 0.5~0.95 的理想工作区
```

同样的信息,蛋糕终于按贡献分了:最有用的 token 拿到最没用那个的 11 倍。

### 第三步(关键对照):换一批大 1000 倍的分数,两种方法各发生什么?

假设换了个模型 / 换了个 RM,梯度天生大 1000 倍:

```
d' = [1.0, 1.2, 0.8]
```

| | 作者链路(÷σ 后直接 softmax) | 方案 B(标准化后 softmax) |
|---|---|---|
| 处理后 | [0.27, 0.32, 0.22] | [0, +1.22, −1.22] |
| softmax 权重 | [0.333, 0.350, 0.317] ← 勉强有点区分 | [0.21, 0.72, 0.06] |
| ESS | 0.999 ← 还是基本平分 | **0.58 ← 和上一批完全一样** |

这张表就是全部结论:

- **作者链路的结果取决于一个没人控制的物理量**(RM 梯度天生多大):梯度小 1000 倍 → 完全平分;梯度大 1000 倍 → 勉强有点区分。同一套 τ=1,行为完全不同,而且事先猜不出落在哪;
- **方案 B 对输入尺度免疫**:0.001 量级进也好、1000 量级进也好,标准化之后都是同一组数,softmax 行为完全一致。这就是"量纲信息从问题里彻底消失"的意思;
- 反过来说,如果非要修作者这条路,得给每批数据找一个 τ ≈ 0.0001 的魔法常数,而这个数随模型、数据、训练进程漂——这就是为什么不选"标定固定 τ"(方案 A)而选标准化(方案 B)。

## 4. 常见疑问:贡献分方差很小,哪来的 3.7?

σ=3.7 和 d=0.001 **量的不是同一个东西,不矛盾**:

| | σ = 3.7 | d ≈ 0.001 |
|---|---|---|
| 量什么 | 8 条回答**最终得分**的差距(奖励轴) | 单个 token 的 logit 动一下,**某一条**回答的得分动多少(敏感度轴) |
| 类比 | 两座山相差 3700 米 | 你每迈一步只能爬高 1 毫米 |
| 谁决定 | RM 打分的跨度 | RM 梯度的陡峭程度 |

"8 条回答分差大"和"单个 token 撬不动分数"同时成立:分数由几百个 token、几千个维度摊出来,单 token 的杠杆天然是千分位。**贡献分自身的离散度很小(例子里 std≈0.000163)——这恰恰就是 softmax 平掉的原因。**

例子里共出现三个离散度,各归其位:

```
① σ = 3.7            奖励的离散度 —— 作者第一步除的尺子(量级:奖励)
② d 的 std ≈ 0.000163  贡献分的离散度 —— B 第二步除的尺子(量级:梯度)
③ softmax 需要的离散度 ≈ 1 —— 目标
```

把 ② 拉到 ③,除数必须与差值同量级(0.0001 档);用 ①(3.7)当除数等于拿"米尺"去量"毫米"——除完是 4×10⁻⁵,离目标更远。**不是贡献分差距大,是归一化的参照物选错了轴。**

## 5. 实证:p9c 的测量和这个例子完全一致

从 p9c 实测的 ESS=0.998 反推(ESS 与 softmax 输入离散度的关系,小离散度下近似成立):

- ESS ≈ 1 − Var(输入) → Var(A·d/τ) ≈ 0.002 → **离散度 ≈ 0.045**

softmax 需要"适度集中"的话输入离散度要到 1~2。也就是说,**作者归一化做完之后,还差大约 20~100 倍**——这正是上面例子里"0.00005 的差值"和"O(1) 的差值"之间的距离,和 τ=1 睡着、VPO 退化成 GRPO 的现场一致。

## 6. 方案 B 与作者归一化的关系:完备化,不是否定

一个可以精确证明的事实:**σ 在方案 B 里会被完全抵消。** 原因:σ 是逐响应常数(同组 8 条回答共享),而 B 的标准化也是逐响应的。上面例子里的数字直接演示了这一点:

```
原始:        d   = [0.0010,  0.0012,  0.0008]  → 标准化 → [0, +1.22, −1.22]
作者归一化:  d/σ = [0.00027, 0.00032, 0.00022]  → 标准化 → [0, +1.22, −1.22]  ← 一模一样
```

先除 σ 再标准化,和直接标准化,结果一字不差。这意味着:

1. **B 完全包含作者的归一化**——"reward 单位 → advantage 单位"的换算在数学上被吸收了;
2. 作者归一化带来的稳健性(RM 换成分数尺度差 10 倍的,σ 会自动补偿)**原样保留**;
3. B 补上的是缺的那半:**残差量级**(来自 RM 梯度陡度)也被校准到 1,softmax 的温度从此真正有效。

## 7. 给作者的一句话版本

> "你的 /σ 是对的,它把 reward 单位换算成了 advantage 单位;但 softmax 只看差值、不看比值,等比缩放保住了比值却没放大差值——输入的绝对量级还差 20~100 倍,ESS 实测 0.998 是直接证据。方案 B(逐响应标准化)把差值本身拉回 O(1),你的归一化在它里面被精确抵消,等于被包含而不是被替换。"

## 一页总结

| 问题 | 答案 |
|---|---|
| 作者说的 RM 归一化存在吗 | 存在:RM 梯度除以组内 σ,`数学原理.md` §4 / `工程实现.md` / `core.py:126` 三处一致 |
| 那诊断还成立吗 | 成立:σ 归一化是等比缩放,解决"单位"但把"差值"越缩越小;softmax 只看差值——例子里 20% 的真实差距在作者链路下权重只差万分之一(ESS≈1),方案 B 下权重差 3~11 倍(ESS 0.58) |
| 为什么 B 不会重蹈覆辙 | 标准化把差值本身校准到 O(1),与输入尺度无关:0.001 量级和 1000 量级进 B,softmax 行为完全一致 |
| 和方案 B 什么关系 | B 是它的完备化:σ 作为逐响应常数在标准化中精确抵消,作者的归一化被包含、稳健性保留 |
| reward.py 里"checkpoint 级 normalization"的悬案 | 已查:Skywork 输出是裸 logit 头(分数 0.77~10.8),无内置归一化 |
