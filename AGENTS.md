# Project storage

The user requires project data and generated artifacts to stay within these approved roots:

- `/mnt/shared-storage-user/ma4agi-gpu/suminle/interests/VPO-RM`
- `/data/VPO-RM`

Store experiment records, exported figures, CSV files, analysis snapshots, and other project artifacts only inside those roots. Do not copy them into `/mnt/data` or other directories outside these roots for chat attachments or convenience. Link to the approved project paths instead. Keep temporary project analysis outputs inside an approved root as well.

Do not move or delete unrelated files when cleaning up an accidental export. Verify the destination contents before deleting a source copy.
