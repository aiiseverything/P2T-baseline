# τ 失配问题与修复方案(通俗版)

> 目标读者:了解 GRPO、但不想啃公式的人。全文只用具体数字说话,公式能不用就不用。
> 对应代码:`vpo_rm/core.py` 的 `allocate()` / `compute_credit()`;诊断数据来自 p9c 两个 run 的 `metrics.jsonl`。

---

## 1. 背景:VPO 本来想干什么?

先看 GRPO 的做法:

- 一个 prompt 采样 8 条回答,RM 打分,算出组内平均分和标准差。
- 每条回答得到一个**序列级分数 A**(高于平均是正,低于平均是负)。
- 这条回答里的**每一个 token 拿到的是同一个 A**。开头第一个字和最关键的那个论点,拿到的学习信号一模一样。

VPO 的想法:**能不能把 A 在 token 之间重新分配?** 好的回答(A>0)里,把学习信号多给"最能把 RM 分数推高的 token";坏的回答(A<0)里,把惩罚多压在"最拖分的 token"上。

怎么判断哪个 token 重要?用 RM 的梯度:对每个 token 问一句——"如果把你的概率调高一点,RM 分数预计会变多少?"这个预测值就是每个 token 的**贡献分**(代码里叫 d_t)。这个思路本身没问题,是这篇工作的核心创意。

## 2. 原方案是怎么分配的(以及 τ 在哪)

分配规则是一个带温度的 softmax,用人话说就是**分蛋糕**:

- 每个 token 按贡献分领蛋糕;
- **温度 τ 越高 → 越接近平分**(每个人拿 1/T);
- **温度 τ 越低 → 贡献分高的拿越多**。

τ=1 时按直觉应该"适度集中"。方案还保证了三条安全性质:

1. 每条回答的蛋糕总量不变(平均还是 A)——不改变学习信号总量,只改变分布;
2. A=0 的回答,信号为零;
3. τ 取极大时,严格退回 GRPO——所以理论上"开最温和"时不会比 GRPO 差。

## 3. 产生了什么问题:蛋糕实际被完全平分了

### 3.1 实测证据

每个 rollout 我们都记录了一个指标 **ESS ratio**(下面解释),它衡量"蛋糕分得多平":

| ESS 值 | 含义 |
|---|---|
| 1.0 | 完全平分 → **VPO 严格等于 GRPO,机制没起作用** |
| 0.5 ~ 0.95 | 有意义地集中,理想工作区 |
| 接近 0 | 蛋糕被一两个 token 独吞,训练会不稳定 |

p9c 的 VPO run,110 个 rollout 的实测:**平均 0.998,最低 0.98,没有一步低于 0.95**。

结论:这 110 步里,VPO 就是 GRPO 加不到 2% 的扰动。这直接解释了为什么两条 reward 曲线完全咬死——不是"两个方法效果一样",而是**其中一个方法根本没启动**。

### 3.2 为什么会这样:单位错了,不是代码错了

softmax 这个"分蛋糕规则"有个隐含假设:进来的分数是 **1 这个量级**。它对量级极其敏感,举个具体例子:

```
假设 3 个 token 的贡献分是 [0.010, 0.012, 0.008](量级千分位,真实情况更小)

温度 τ=1:  权重 = [0.334, 0.335, 0.333]   ← softmax 看不出差别,平分
温度 τ=0.01: 权重 = [0.34, 0.42, 0.24]    ← 同样的数据,开始分化
```

而我们算出来的贡献分,量级是多少?它是一串小量连乘出来的:RM 对输入的梯度(~千分位)× embedding 内积,再除以组内标准差(≈3.7)。最终 **d_t 的自然量级是 0.001~0.01**。

τ=1 是按"分数是 O(1)"拍的,实际进来的是 0.001——**差了两三个数量级**。softmax 面对一排几乎相等的分数,只能平分。就像考试满分 100 的规则,拿来给一群都考了 0.001 分的人排名,排名失效,只能并列第一。

关键在于:这条计算链上(RM 梯度多大、embedding 多长、组内 σ 多大)**没有任何一环把 d_t 校准到统一量级**。换模型、换数据、训练中策略变化,量级都会漂。这不是某个 bug,是设计上缺了一个"归一化"步骤。

## 4. 解决方案 B:进 softmax 之前,先把分数标准化

### 4.1 做法(一句话)

**每条回答内部,先把贡献分换算成"标准分"(减均值、除以标准差),再进 softmax。**

类比:三门考试(回答 A/B/C)卷面分分别是百分制、千分制、万分制,直接比绝对分没意义。先各自换算成"比本卷平均高几个标准差",就可以放在一起比了。这一步之后,softmax 看到的分数永远是 O(1) 量级,温度旋钮才真正有效。

换算后温度改名叫 τ'(无量纲),含义变得干净:

- τ'=1 → 分数的离散度(标准差)恰好是 1 → **适度集中**,预计 ESS 落在 0.6~0.9,正好处在工作区;
- τ'=2 → 更平;τ'=0.5 → 更集中。

### 4.2 之前的三条安全性质,一条都不丢

1. 蛋糕总量不变(平均仍是 A);
2. A=0 仍全零;
3. τ' 取极大仍严格退回 GRPO。

GRPO 那条对照 run 的代码路径完全不受影响。

### 4.3 要诚实说的一点代价

《数学原理》§5 是从"KL 正则化的分配问题"严格推导出原公式的,那个推导里 τ 还耦合了学习率。标准化相当于把分配问题换了个参数化方式,**推导的思路(一阶展开 + KL 稳定化)依然成立,但严格解的形式变了**。另外正因为原推导里 τ 和学习率耦合,你这次 lr 从 1e-6 改到 1e-4(变了 100 倍),固定 τ 的"平衡点"本来也得跟着动——标准化恰好把这个耦合解开,这是选 B 而不是"标定一个固定 τ"的另一个理由。

## 5. 会带来哪些变化

### 5.1 训练动态上

- **VPO 和 GRPO 从此真正不同**。同样一条高质量回答,GRPO 里 500 个 token 每个 +0.3 信号;VPO 里可能是"关键论点的 token +1.5,客套话 token +0.05"。
- 学习信号的**分布更尖了**,方差变大——这是机制本身的代价,所以配了两道保险(见 5.2)。
- 期望的效果路径:同样的算力和步数下收敛更快、最终 eval 更好。**注意:训练 reward 曲线的水位不会因此变高**(总量守恒),不要拿曲线高低来判断 VPO 好坏,要看固定验证集和 benchmark。

### 5.2 配套的护栏和监控

| 项目 | 内容 |
|---|---|
| 权重上限 | 单 token 权重最多 20(约平均值的 20 倍),超出截断后重新归一。防止个别回答里 softmax 偶发尖峰,把某个 token 的 advantage 打爆(梯度裁剪管不到这里,它作用在梯度上,这个问题在 advantage 上) |
| ESS 双向报警 | ESS > 0.99 → 打警告"VPO 未激活"(就是这次踩的坑);ESS < 0.05 → 打警告"credit 塌缩" |
| 新增指标 | 每个 rollout 记录:ESS(已有,但要真的看)、原始分数的离散度 std(监控量纲问题是否复发)、最大权重均值 |

### 5.3 流程上

正式跑 500 步之前,先花 3~5 个 rollout **扫 τ' ∈ {0.25, 0.5, 1, 2}**,确认 ESS 落点和权重分布形状,选一个中间偏稳的值。不要在正式 run 里第一次点亮这个机制。

### 5.4 代码改动量

集中在 `core.py` 的 `allocate()`:标准化两行 + 权重截断几行;metrics 加三个字段。不碰 GRPO 路径,不碰生成和 RM 前向。

---

## 一页总结

| 问题 | 答案 |
|---|---|
| 原方案是什么 | 用 RM 梯度给每个 token 算"贡献分",softmax 按贡献分把序列 advantage A 分给各 token,τ 控制集中程度 |
| 什么问题 | 贡献分的自然量级是 0.001,而 τ=1 按 O(1) 设计;softmax 分不出差别,蛋糕被平分,ESS≈0.998,**VPO 实际从未启动** |
| 为什么不行 | 计算链上没有任何环节把贡献分校准到统一量级;换模型/数据/lr 都会再漂 |
| 怎么解决(B) | softmax 前对贡献分做逐回答标准化(减均值除标准差),温度变无量纲 τ';τ'=1 即适度集中 |
| 带来什么变化 | VPO 与 GRPO 真正分化;学习信号更尖(配权重上限 20 + ESS 报警兜底);训练曲线水位不变,收益要看收敛速度和最终 eval;正式跑前先小扫参 |
